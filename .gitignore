node_modules/
.next/

# Cloudflare Workers (OpenNext) build output / local state / secrets
.open-next/
.wrangler/
cloudflare-env.d.ts
.dev.vars
out/
dist/
.qa-dist/
.learning-test-dist/
.learning-isolation-dist/
.batch17-final-dist/
.audio-test-dist/
.mobile-route-qa/
.qa/
.qa-playroom/
.env
.env.local
*.log
*.zip
coverage/
playwright-report/
test-results/
public/models/*.task
public/mediapipe/wasm/*.wasm
public/mediapipe/wasm/*.js

# Third-party affiliate imagery is local-only by default. A reviewed owned/licensed
# asset may be force-added only after its provenance entry is approved.
public/affiliate/*.jpg
public/affiliate/*.jpeg
public/affiliate/*.png
public/affiliate/*.webp
public/affiliate/*.avif
public/affiliate/*.svg

# Character production binaries are also fail-closed by default.
# After provenance + visual approval, add the exact reviewed file with git add -f.
public/artwork/characters/*.jpg
public/artwork/characters/*.jpeg
public/artwork/characters/*.png
public/artwork/characters/*.webp
public/artwork/characters/*.avif
public/artwork/characters/*.svg

# Pre-generated narration is production-only and must pass the narration
# provenance + human-review gate before being force-added to the public tree.
public/audio/narration/en/*.mp3
public/audio/narration/en/*.wav
public/audio/narration/en/*.ogg
public/audio/narration/en/*.opus
public/audio/narration/en/*.aac
public/audio/narration/en/*.m4a
public/audio/narration/en/*.flac

# local-only, not served by Next.js
internal/

# stale backup archives / scratch
_to_delete/
_snap2.tgz
_mlh_snapshot.tgz
tsconfig.tsbuildinfo
