import assert from "node:assert/strict";
import { readFileSync, rmSync } from "node:fs";
import { createRequire } from "node:module";
import { spawnSync } from "node:child_process";
import path from "node:path";
import process from "node:process";
const root=process.cwd(); const outDir=path.join(root,".learning-test-dist"); rmSync(outDir,{recursive:true,force:true}); const tscBin=path.join(root,"node_modules","typescript","bin","tsc"); const compile=spawnSync(process.execPath,[tscBin,"-p","tsconfig.learning-tests.json"],{cwd:root,stdio:"inherit"}); if(compile.status!==0)process.exit(compile.status??1);
const require=createRequire(import.meta.url); const system=require(path.join(outDir,"src","lib","learning","system.js")); const curriculum=require(path.join(outDir,"src","lib","learning","curriculum.js")); const catalog=require(path.join(outDir,"src","lib","learning","catalog.js")); const manifest=require(path.join(outDir,"src","lib","learning","contentManifest.js")); const batch7=require(path.join(outDir,"src","lib","learning","mathBatch7.js")); const batch8=require(path.join(outDir,"src","lib","learning","bahasaBatch8.js")); const batch9=require(path.join(outDir,"src","lib","learning","englishBatch9.js")); const batch10=require(path.join(outDir,"src","lib","learning","iqroBatch10.js")); const batch11=require(path.join(outDir,"src","lib","learning","lettersBatch11.js")); const batch12=require(path.join(outDir,"src","lib","learning","logicBatch12.js")); const batch13=require(path.join(outDir,"src","lib","learning","scienceBatch13.js"));
const expansionTargets=Object.freeze({bahasa:100,english:100,math:100,iqro:100,letters:100,logic:100,science:100,color:100}); const floors=Object.freeze({bahasa:100,english:100,math:100,iqro:100,letters:100,logic:100,science:50,color:2}); const countBy=(items,key)=>items.reduce((c,i)=>{c[i[key]]=(c[i[key]]??0)+1;return c;},{}); const batch11PracticeIds=new Set(["letters-stroke-vertical","letters-stroke-horizontal","letters-stroke-diagonal","letters-trace-g-practice","letters-trace-i-practice","letters-trace-m-practice","letters-trace-n-practice","letters-trace-o-practice","letters-trace-t-practice","letters-trace-u-practice","letters-trace-w-practice","letters-trace-z-practice"]);
try {
 const coverage=curriculum.getCurriculumCoverage(); assert.deepEqual(coverage.uncoveredStageIds,[]); assert.deepEqual(coverage.uncoveredActivityIds,[]); const activityCounts=countBy(system.ACTIVITIES,"subjectId"); const runtimeCounts=countBy(system.ACTIVITIES,"runtime"); const assessmentCounts=countBy(Object.values(catalog.ACTIVITY_LEARNING_SPECS),"assessment");
 assert.equal(system.SUBJECTS.length,8); assert.equal(system.ACTIVITIES.length,652,"Batch 13 Wave B must raise the catalog to 652 total activities"); assert.equal(system.STAGES.length,36,"Batch 13 Wave B must raise the catalog to 36 stages"); assert.equal(curriculum.LEARNING_PATHS.length,8); assert.equal(curriculum.LEARNING_LESSONS.length,147,"Batch 13 Wave B must raise the catalog to 147 lessons"); assert.equal(manifest.CONTENT_PACKS.length,147,"Batch 13 Wave B must raise the catalog to 147 packs"); assert.equal(catalog.LEARNING_SKILLS.length,150,"Batch 13 Wave B must raise the catalog to 150 skills"); for(const [id,floor] of Object.entries(floors))assert.ok((activityCounts[id]??0)>=floor,`${id} regressed below ${floor}`); for(const id of ["math","bahasa","english","iqro","letters","logic"])assert.equal(activityCounts[id],100,`${id} must remain exactly 100 during Batch 13`); assert.equal(activityCounts.science,50,"Batch 13 Wave B must raise Science to exactly 50 activities"); assert.deepEqual(batch7.MATH_BATCH7_WAVE_ACTIVITY_COUNTS,{A:18,B:25,C:25,D:25}); assert.deepEqual(batch8.BAHASA_BATCH8_WAVE_ACTIVITY_COUNTS,{A:19,B:25,C:25,D:25}); assert.deepEqual(batch9.ENGLISH_BATCH9_WAVE_ACTIVITY_COUNTS,{A:19,B:25,C:25,D:25}); assert.deepEqual(batch10.IQRO_BATCH10_WAVE_ACTIVITY_COUNTS,{A:21,B:25,C:25,D:25}); assert.deepEqual(batch11.LETTERS_BATCH11_WAVE_ACTIVITY_COUNTS,{A:22,B:25,C:25,D:25}); assert.deepEqual(batch12.LOGIC_BATCH12_WAVE_ACTIVITY_COUNTS,{A:22,B:25,C:25,D:25}); assert.deepEqual(batch13.SCIENCE_BATCH13_WAVE_ACTIVITY_COUNTS,{A:22,B:25}); assert.equal(batch11.LETTERS_BATCH11_ACTIVITY_IDS.length,97); assert.equal(batch12.LOGIC_BATCH12_ACTIVITY_IDS.length,97); assert.equal(batch13.SCIENCE_BATCH13_ACTIVITY_IDS.length,47); assert.equal(new Set(batch13.SCIENCE_BATCH13_ACTIVITY_IDS).size,47);
 for(const id of ["math","bahasa","english","iqro","letters","logic"])assert.equal(activityCounts[id],expansionTargets[id]); for(const id of ["science","color"])assert.ok((activityCounts[id]??0)<expansionTargets[id],`${id} reached 100 unexpectedly during Batch 13 Wave B`);
 assert.deepEqual(runtimeCounts,{tap_choice:441,listen_and_choose:76,matching:115,trace:14,story:1,motion_game:3,coloring:2}); assert.deepEqual(assessmentCounts,{assessed:633,practice:19}); for(const id of batch11.LETTERS_BATCH11_ACTIVITY_IDS){const spec=catalog.getActivityLearningSpec(id); assert.ok(spec,`${id} needs a learning spec`); if(batch11PracticeIds.has(id)){assert.equal(spec.assessment,"practice"); assert.equal(spec.requiredForStage,false);}else assert.equal(spec.assessment,"assessed");} for(const id of batch12.LOGIC_BATCH12_ACTIVITY_IDS){const spec=catalog.getActivityLearningSpec(id); assert.ok(spec,`${id} needs a learning spec`); assert.equal(spec.assessment,"assessed");} for(const id of batch13.SCIENCE_BATCH13_ACTIVITY_IDS){const spec=catalog.getActivityLearningSpec(id); assert.ok(spec,`${id} needs a learning spec`); assert.equal(spec.assessment,"assessed",`${id} must use measured assessed evidence`);} const scienceActivities=system.ACTIVITIES.filter((activity)=>activity.subjectId==="science"); const scienceSpecs=scienceActivities.map((activity)=>catalog.getActivityLearningSpec(activity.id)); assert.equal(scienceSpecs.filter((spec)=>spec?.assessment==="assessed").length,50); assert.equal(scienceSpecs.filter((spec)=>spec?.assessment==="practice").length,0); const audio=readFileSync(path.join(root,"src","lib","audio","AudioManager.ts"),"utf8"); assert.match(audio,/mainlagi-speech-latency/); console.log("Mainlagi Expansion Batch 13 Science Wave B contracts passed.");
} catch(error){console.error(error);process.exit(1);}
