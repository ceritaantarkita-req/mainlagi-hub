import assert from "node:assert/strict";
import { readFileSync, rmSync } from "node:fs";
import { createRequire } from "node:module";
import { spawnSync } from "node:child_process";
import path from "node:path";
import process from "node:process";
const root=process.cwd(); const outDir=path.join(root,".learning-test-dist"); rmSync(outDir,{recursive:true,force:true}); const tscBin=path.join(root,"node_modules","typescript","bin","tsc"); const compile=spawnSync(process.execPath,[tscBin,"-p","tsconfig.learning-tests.json"],{cwd:root,stdio:"inherit"}); if(compile.status!==0)process.exit(compile.status??1);
const require=createRequire(import.meta.url); const system=require(path.join(outDir,"src","lib","learning","system.js")); const curriculum=require(path.join(outDir,"src","lib","learning","curriculum.js")); const catalog=require(path.join(outDir,"src","lib","learning","catalog.js")); const manifest=require(path.join(outDir,"src","lib","learning","contentManifest.js")); const batch7=require(path.join(outDir,"src","lib","learning","mathBatch7.js"));
const expansionTargets=Object.freeze({bahasa:100,english:100,math:100,iqro:100,letters:100,logic:100,science:100,color:100}); const floors=Object.freeze({bahasa:6,english:6,math:75,iqro:4,letters:3,logic:3,science:3,color:2}); const countBy=(items,key)=>items.reduce((c,i)=>{c[i[key]]=(c[i[key]]??0)+1;return c;},{});
try {
 const coverage=curriculum.getCurriculumCoverage(); assert.deepEqual(coverage.uncoveredStageIds,[]); assert.deepEqual(coverage.uncoveredActivityIds,[]); const activityCounts=countBy(system.ACTIVITIES,"subjectId"); const runtimeCounts=countBy(system.ACTIVITIES,"runtime"); const assessmentCounts=countBy(Object.values(catalog.ACTIVITY_LEARNING_SPECS),"assessment");
 assert.equal(system.SUBJECTS.length,8); assert.equal(system.ACTIVITIES.length,102); assert.equal(system.STAGES.length,13); assert.equal(curriculum.LEARNING_PATHS.length,8); assert.equal(curriculum.LEARNING_LESSONS.length,29); assert.equal(manifest.CONTENT_PACKS.length,29); assert.equal(catalog.LEARNING_SKILLS.length,32); for(const [id,floor] of Object.entries(floors))assert.ok((activityCounts[id]??0)>=floor,`${id} regressed below ${floor}`); assert.equal(activityCounts.math,75); assert.deepEqual(batch7.MATH_BATCH7_WAVE_ACTIVITY_COUNTS,{ A: 18, B: 25, C: 25 }); assert.equal(batch7.MATH_BATCH7_ACTIVITY_IDS.length,68); assert.equal(new Set(batch7.MATH_BATCH7_ACTIVITY_IDS).size,68);
 for (const [subjectId,target] of Object.entries(expansionTargets)) assert.ok((activityCounts[subjectId]??0)<target,`${subjectId} reached 100 unexpectedly during Wave C`);
 assert.deepEqual(runtimeCounts,{tap_choice:68,listen_and_choose:5,matching:21,trace:2,story:1,motion_game:3,coloring:2}); assert.deepEqual(assessmentCounts,{assessed:95,practice:7}); for(const id of batch7.MATH_BATCH7_ACTIVITY_IDS)assert.equal(catalog.getActivityLearningSpec(id)?.assessment,"assessed",`${id} must remain assessed with measured evidence`); const audio=readFileSync(path.join(root,"src","lib","audio","AudioManager.ts"),"utf8"); assert.match(audio,/mainlagi-speech-latency/); console.log("Mainlagi Expansion Batch 7 Math Wave C contracts passed.");
} catch(error){console.error(error);process.exit(1);}
