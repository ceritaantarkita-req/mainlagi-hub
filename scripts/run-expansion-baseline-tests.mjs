import assert from "node:assert/strict";
import { readFileSync, rmSync } from "node:fs";
import { createRequire } from "node:module";
import { spawnSync } from "node:child_process";
import path from "node:path";
import process from "node:process";
const root=process.cwd(); const outDir=path.join(root,".learning-test-dist"); rmSync(outDir,{recursive:true,force:true}); const tscBin=path.join(root,"node_modules","typescript","bin","tsc"); const compile=spawnSync(process.execPath,[tscBin,"-p","tsconfig.learning-tests.json"],{cwd:root,stdio:"inherit"}); if(compile.status!==0)process.exit(compile.status??1);
const require=createRequire(import.meta.url); const system=require(path.join(outDir,"src","lib","learning","system.js")); const curriculum=require(path.join(outDir,"src","lib","learning","curriculum.js")); const catalog=require(path.join(outDir,"src","lib","learning","catalog.js")); const manifest=require(path.join(outDir,"src","lib","learning","contentManifest.js")); const batch7=require(path.join(outDir,"src","lib","learning","mathBatch7.js")); const batch8=require(path.join(outDir,"src","lib","learning","bahasaBatch8.js"));
const expansionTargets=Object.freeze({bahasa:100,english:100,math:100,iqro:100,letters:100,logic:100,science:100,color:100}); const floors=Object.freeze({bahasa:100,english:6,math:100,iqro:4,letters:3,logic:3,science:3,color:2}); const countBy=(items,key)=>items.reduce((c,i)=>{c[i[key]]=(c[i[key]]??0)+1;return c;},{});
try {
 const coverage=curriculum.getCurriculumCoverage(); assert.deepEqual(coverage.uncoveredStageIds,[]); assert.deepEqual(coverage.uncoveredActivityIds,[]); const activityCounts=countBy(system.ACTIVITIES,"subjectId"); const runtimeCounts=countBy(system.ACTIVITIES,"runtime"); const assessmentCounts=countBy(Object.values(catalog.ACTIVITY_LEARNING_SPECS),"assessment");
 assert.equal(system.SUBJECTS.length,8); assert.equal(system.ACTIVITIES.length,221,"Batch 8 Wave D must expose 221 total activities"); assert.equal(system.STAGES.length,18,"Batch 8 Wave D must expose 18 stages"); assert.equal(curriculum.LEARNING_PATHS.length,8); assert.equal(curriculum.LEARNING_LESSONS.length,54,"Batch 8 Wave D must expose 54 lessons"); assert.equal(manifest.CONTENT_PACKS.length,54,"Batch 8 Wave D must expose 54 packs"); assert.equal(catalog.LEARNING_SKILLS.length,57,"Batch 8 Wave D must expose 57 skills"); for(const [id,floor] of Object.entries(floors))assert.ok((activityCounts[id]??0)>=floor,`${id} regressed below ${floor}`); assert.equal(activityCounts.math,100,"Batch 7 Math target must remain closed at exactly 100"); assert.equal(activityCounts.bahasa,100,"Batch 8 must close Bahasa at exactly 100 activities"); assert.deepEqual(batch7.MATH_BATCH7_WAVE_ACTIVITY_COUNTS,{ A: 18, B: 25, C: 25, D: 25 }); assert.deepEqual(batch8.BAHASA_BATCH8_WAVE_ACTIVITY_COUNTS,{ A: 19, B: 25, C: 25, D: 25 }); assert.equal(batch8.BAHASA_BATCH8_ACTIVITY_IDS.length,94); assert.equal(new Set(batch8.BAHASA_BATCH8_ACTIVITY_IDS).size,94);
 assert.equal(activityCounts.math,expansionTargets.math); assert.equal(activityCounts.bahasa,expansionTargets.bahasa); for (const [subjectId,target] of Object.entries(expansionTargets)) if(subjectId!=="math" && subjectId!=="bahasa") assert.ok((activityCounts[subjectId]??0)<target,`${subjectId} reached 100 unexpectedly during Batch 8`);
 assert.deepEqual(runtimeCounts,{tap_choice:147,listen_and_choose:24,matching:42,trace:2,story:1,motion_game:3,coloring:2}); assert.deepEqual(assessmentCounts,{assessed:214,practice:7}); for(const id of batch8.BAHASA_BATCH8_ACTIVITY_IDS)assert.equal(catalog.getActivityLearningSpec(id)?.assessment,"assessed",`${id} must remain assessed with measured evidence`); const bahasaActivities=system.ACTIVITIES.filter((activity)=>activity.subjectId==="bahasa"); const bahasaSpecs=bahasaActivities.map((activity)=>catalog.getActivityLearningSpec(activity.id)); assert.equal(bahasaSpecs.filter((spec)=>spec?.assessment==="assessed").length,99,"Bahasa final must have 99 assessed activities"); assert.equal(bahasaSpecs.filter((spec)=>spec?.assessment==="practice").length,1,"Bahasa historical story remains the single practice activity"); const audio=readFileSync(path.join(root,"src","lib","audio","AudioManager.ts"),"utf8"); assert.match(audio,/mainlagi-speech-latency/); console.log("Mainlagi Expansion Batch 8 Bahasa Wave D final contracts passed.");
} catch(error){console.error(error);process.exit(1);}
